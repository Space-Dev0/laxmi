ssh -t -i "laxmi.pem" ubuntu@ec2-52-205-10-59.compute-1.amazonaws.com "grep -q 'set -g mouse on' ~/.tmux.conf || echo -e 'set -g mouse on\nset -s set-clipboard on' >> ~/.tmux.conf; tmux new-session -A -s laxmi_bot ""bash -l -c 'cd laxmi/; micro -clipboard terminal config.yaml; uv run main.py; exec bash'"""
